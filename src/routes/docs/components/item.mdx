---
title: Item
description: A versatile list item component with support for icons, descriptions, and actions.
---

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="item-demo" />

:::

:::tab[Code]

```file="~/registry/examples/item-demo" frame=none showLineNumbers

```

:::
::::

## Installation

### CLI

```package-exec
shadcn@latest add @gc-solid/item
```

### Manual

Install the following dependencies:

```package-install
lucide-solid
```

Copy and paste the following code into your project.

```file="~/registry/ui/item.tsx" showLineNumbers

```

This component uses the `separator` component from the registry. Make sure you have it installed.


### CSS Setup

<Callout>
**Manual CSS Setup Required** — The shadcn CLI installs component code and dependencies but cannot update your CSS configuration. This component has no additional CSS requirements beyond the base theme setup. See the [Installation Guide](/docs/getting-started/installation) for the base `app.css` template.
</Callout>

## Usage

```tsx
import { Item, ItemIndicator, ItemLabel } from "~/components/ui/item";
```

```tsx
<Item>
  <ItemIndicator>{/* Icon */}</ItemIndicator>
  <ItemLabel>Item label</ItemLabel>
</Item>
```
