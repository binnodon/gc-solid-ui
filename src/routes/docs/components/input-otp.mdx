---
title: Input OTP
description: A one-time password input component for entering verification codes.
---

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="input-otp-demo" />

:::

:::tab[Code]

```file="~/registry/examples/input-otp-demo" frame=none showLineNumbers

```

:::
::::

## Installation

### CLI

```package-exec
shadcn@latest add @gc-solid/input-otp
```

### Manual

Copy and paste the following code into your project.

```file="~/registry/ui/input-otp.tsx" showLineNumbers

```


### CSS Setup

<Callout>
**Manual CSS Setup Required** — The shadcn CLI installs component code and dependencies but cannot update your CSS configuration. This component has no additional CSS requirements beyond the base theme setup. See the [Installation Guide](/docs/getting-started/installation) for the base `app.css` template.
</Callout>

## Usage

```tsx
import { InputOTP } from "~/components/ui/input-otp";
```

```tsx
<InputOTP maxLength={6} />
```
